struct pos
{
	int x,y=0;
};

struct pos2
{
	float x, y;
};

float func(struct pos a, struct pos b)
{
	if(a.x == b.x && a.y == b.y)
		return 1.1;
	else
		return 0.2;
}

int func();

int main()
{
	struct pos arr[10];
	int array[10];
	int ans;
}
